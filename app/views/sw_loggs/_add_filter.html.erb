<%= form_for :sw_filter, url: sw_loggs_path, :html => {:class=>"form-inline"}, remote: true do |f|%>
    <div class="form-group">
      <%= f.label :name, 'Имя фильтра' %>
      <%= f.text_field :name, :class=>"form-control input-sm" %>
    </div>

    <%= f.submit "Добавить", :class=>"btn btn-default"%>

<% end %>